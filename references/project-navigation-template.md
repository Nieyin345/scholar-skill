# 00_项目导航

> 本文件是 scholar 在本项目的「目录索引 + 文档管理」入口（项目导航文档）——**文件结构/命名规则/文献与文档位置的唯一事实来源**。
> 链接约定：本文件及本主题下所有生成文档都在 Obsidian vault 中，文档互引一律用 Obsidian wikilink（`[[文件名]]` / `[[文件名|显示文本]]`），如 `[[01_检索结果]]`、`[[03_文献综述]]`、论文 `[[论文/<主题>/<论文短名-年>/<md文件名>]]`。
> 规则：**触发 scholar 任一流程时检查/生成；只有文件发生变动才更新**（创建 / 删除 / 重命名 / 移动 / 转换完成；详见项目根 `AGENTS.md` 与 `SKILL.md`「项目导航文档」章节）。
> 复制本模板到 `论文/<主题>/00_项目导航.md` 后按实际填写；未用到的区块保留标题并标注「—」。

## 1. 项目信息

| 项 | 内容 |
|----|------|
| 主题 | <检索主题> |
| 研究方向（一句话） | <核心问题/主张> |
| 创建日期 | YYYY-MM-DD |
| 最近更新 | YYYY-MM-DD |
| 项目根目录 | 论文/<主题>/ |

## 2. 文件结构（目录总表，唯一事实来源）

```
论文/<主题>/
├── 00_项目导航.md            ← 本文件（入口，文件变动时更新）
├── 00_研究方向.md            ← 流程0 产出（可选，存在则复用）
├── 00_研究问题卡.md          ← 流程0 产出（可选）
├── 00_文献验证.md            ← 流程0：文献验证记录（可选）
├── 01_检索结果.md            ← 流程一：纳入文献清单（含排除 reason）
├── 02_检索日志.md            ← 流程一：检索式/日期/命中数（可复现）
├── papers.jsonl             ← 可选：结构化文献库（机器可读，与 01 互补）
├── 03_文献综述.md             ← 流程一：综述（置顶文档区，编号与 01/02 连续）
├── thesis/                 ← 流程四：论文 LaTeX 模板（用户模板或默认模板副本，目录名固定 thesis）
├── figures/                 ← 科研示意图（科研插图工具产出：可编辑源 + SVG/PDF 导出）
├── <论文短名-年>/            ← 每篇论文一个文件夹：pdf + md（流程一，下载即转换）
│   ├── <论文短名-年>.pdf
│   └── <论文短名-年>.md      ← 转换稿（源；学习笔记的 source 指回这里）
├── 解决方案/                 ← 流程二（目录内编号独立于根目录）
│   ├── 01_场景分析与问题剖析.md
│   ├── 02_实现逻辑与方案设计-方案X.md   ← 多方案逐篇
│   ├── 03_代码架构设计.md
│   └── README.md             ← 方案决策日志
├── implementation/           ← 流程三：代码实现（与 解决方案/ 并列）
│   ├── (代码、测试…)
│   ├── results/              ← 实验原始指标/日志/配置快照（03 §4）
│   ├── figures/              ← 实验数据图（SVG/PDF/300dpi PNG，03 §4）
│   └── tables/               ← 论文用表（CSV/JSON/LaTeX，03 §4）
└── paper/                    ← 流程四：论文写作
    ├── paper.tex / paper.pdf / references.bib
    └── versions/             ← 历史版本 + review 反馈（只增不删）
```

> 学习笔记不在本目录：写入当前项目 `笔记/` 库（可直接作为 Obsidian vault 打开），论文笔记的 `source` 字段指回本目录的 `<论文短名-年>.md`（转换稿是源，笔记是提炼，不在主题里复制笔记）。

## 3. 命名规则

> **排序约定**：主题文件夹分「置顶文档区」（`00_`/`01_`/`02_`/`03_` 编号文档）与「论文区」（`<论文短名-年>/` 文件夹）；文档区编号置顶，论文文件夹排在文档区之后；综述类（Review/Survey）论文文件夹保持 `<短名>-<年>-<综述关键词>` 命名，与其他论文同区排序。

| 位置/前缀 | 归属 | 规则 |
|-----------|------|------|
| 根目录 `00_` | 流程0 + 全局 | 核心文档：`00_项目导航.md`、`00_研究方向.md`、`00_研究问题卡.md`、`00_文献验证.md` |
| 根目录 `01_`/`02_`/`03_` | 流程一 | 置顶文档区：`01_检索结果.md`、`02_检索日志.md`、`03_文献综述.md`（编号连续，排在论文文件夹之前） |
| 根目录 `01_`、`02_` | 流程一 | `01_检索结果.md`、`02_检索日志.md` |
| 根目录 `<论文短名-年>/` | 流程一 | 每篇论文一个文件夹，pdf + md 同放；短名用「一作姓-年-主题关键词」；转换产物统一命名 `<短名>.md`（转换时传 `--md-name <短名>`） |
| `解决方案/` 内 `01/02/03` | 流程二 | 目录内独立编号，与根目录 `01_/02_` 不混用 |
| `figures/`（根目录） | 科研插图工具 | **示意图**（结构/流程/机理，可编辑源 + 矢量导出） |
| `implementation/figures/` | 流程三 | **实验数据图**（折线/柱状/热图等，代码自动生成）——两类图不混放 |
| `paper/` | 流程四 | 论文 + 版本存档；素材从 `implementation/{results,figures,tables}` 与根目录 `figures/` 引用 |
| `thesis/`（根目录） | 流程四 | 论文 LaTeX 模板固定目录：用户模板或默认模板统一放这，目录名固定 `thesis` |

## 4. 文档索引

| 文档 | 位置 | 所属流程 | 状态 |
|------|------|----------|------|
| 项目导航 | `00_项目导航.md` | 全局 | ✅ 最新 |
| 研究方向 | `00_研究方向.md` | 流程0 | 待生成 / 🔄 进行中 / ✅ 已完成 |
| 检索结果 | `01_检索结果.md` | 流程一 | 待生成 / 🔄 进行中 / ✅ 已完成 |
| 检索日志 | `02_检索日志.md` | 流程一 | 待生成 / ✅ 已记录 |
| 文献综述 | `03_文献综述.md` | 流程一 | 待生成 / ✅ 已完成 |
| 场景分析 | `解决方案/01_…md` | 流程二 | 待生成 / ✅ 已完成 |
| 实现逻辑 | `解决方案/02_…-方案X.md` | 流程二 | 待生成 / ✅ 已完成 |
| 代码架构 | `解决方案/03_…md` | 流程二 | 待生成 / ✅ 已通过评审 |
| 论文 | `paper/paper.tex` | 流程四 | 待生成 / vN 迭代中 / ✅ 可发表 |

> 状态约定：`待生成` / `🔄 进行中` / `✅ 已完成` / `⛔ 已中断（返回流程X）`

## 5. 文献库索引

| 论文（作者 年份 短名） | DOI | 文件夹 | PDF | MD | 综述引用 |
|------------------------|-----|--------|-----|----|---------|
| 例：Smith 2024 方法综述 | 10.xxxx/… | `<论文短名-2024>/` | ✅ | ✅ | ✅ |

> 约定：下载即转换（MinerU）；每篇论文一个文件夹，PDF + MD 同放；转换失败在 MD 列标注原因；状态以 `01_检索结果.md` 筛选结果为准。

## 6. 当前状态

- 当前流程：
- 已完成：
- 下一步：
- 阻塞 / 待用户决定：

## 7. 更新记录

| 日期 | 触发流程 | 变更内容 |
|------|----------|----------|
| YYYY-MM-DD | 流程一 | 生成导航文档；登记检索方案卡 |
| YYYY-MM-DD | 流程三 | 新增 `implementation/`；登记已完成模块 |

## 8. 防混乱铁律（本文件固定章节，勿删）

1. **本导航是文件结构的唯一事实来源**：任何文件/目录以本导航为准，导航与实际不符时先改导航再动文件；
2. **新文件先登记再创建**：创建新文件/目录前，先在 §4 文档索引（或 §5 文献库索引）登记位置，再创建；
3. **移动/重命名必须同步**：文件被移动/重命名时同步更新本导航，不允许「文件在但导航没更新」；
4. **每个文件只属于一个位置**：不复制文件到多个目录；确需复用（如论文引用图）用相对路径/引用，不复制；
5. **两类图不混放**：实验数据图只进 `implementation/figures/`，示意图只进根目录 `figures/`；
6. **转换稿 vs 笔记不混淆**：`<论文短名-年>.md` 是转换稿（源），学习笔记是提炼（写入 vault），不在主题里复制笔记；
7. **未规划目录不新建**：需要新目录时，先更新本导航并征得用户确认后再创建；
8. **文件变动时才更新**：创建/删除/重命名/移动/转换完成等文件变动发生后，才更新 §2 结构、§4/§5 索引、§6 状态、§7 记录（只增不删）；无文件变动的轮次不重写导航。
9. **短名冲突加序号**：论文短名（一作姓-年-主题关键词）冲突时加序号（如 `Smith-2024-ts-2`），不覆盖同名文件夹；
10. **移动论文必同步笔记**：移动/重命名 `<论文短名-年>/` 文件夹时，同步更新 Obsidian 学习笔记的 `source` 字段，避免引用断裂；
11. **中间产物不落地主题**：下载临时文件、MinerU 中间输出、LaTeX 编译 aux 等不进主题根目录，用完即清；确需保留的进对应子目录。
12. **临时脚本/中间产物进 `.scholar_tmp/`**：临时 `_*.py` / `_*.txt` / `_*.json` 一律放 `<项目>/.scholar_tmp/`（init 自动创建，Obsidian 隐藏），会话/阶段结束用 `init_workspace.py cleanup-tmp` 清理；**禁止散落在项目根目录或主题文件夹**。

## 9. 三库导航总览（固定章节，勿删）

| 库 | 导航文件 | 位置 | 维护者 | 更新时机 |
|----|----------|------|--------|----------|
| 论文库（库级） | `00_索引.md` | `<项目>/论文/` | scholar 主流程（`init_workspace.py add-topic` 登记） | 新增/删除研究方向时（文件变动时） |
| 论文库（主题级） | `00_项目导航.md` | `<项目>/论文/<主题>/` | scholar 主流程 + 各流程门禁 | 文件变动时（创建/删除/重命名/移动/转换完成） |
| 书籍库 | `00_索引.md` | `<项目>/书籍/` | 书籍库管理工具（`tools/书籍库管理.md`） | 文件变动时（新增/删除/重命名/移动/转换完成） |
| 学习笔记库 | `00-导航.md` | `<项目>/笔记/` | 学习笔记工具（`tools/学习笔记.md`） | 文件变动时（新增/删除/重命名/移动） |

> 各导航文件各管各库、互不替代：论文库索引管研究方向注册，主题导航管研究方向的文件结构，书籍库导航管教材目录，笔记库导航管知识笔记结构。
> 统一纪律：**只有文件发生变动才更新**（创建/删除/重命名/移动/转换完成），无文件变动的轮次不重写；历史保留在各导航的更新记录中（只增不删）。

