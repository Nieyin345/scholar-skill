# 论文写作规范

版本：v2.3（2026-08-07）
适用：流程四 / 产出：`论文/<主题>/thesis/` 工作目录内的英文论文 `main.tex` + 编译 `main.pdf`（可含中文定稿 `draft-zh.md`）
模板：`论文/<主题>/thesis/`（流程四第2步选定：用户模板或默认模板副本，统一放该目录；默认源 `references/latex-template/main.tex`，IEEE CS Magazine（CsMag）官方模板，仅格式）
执行方式：本规范 + `通用写作规范.md` **同时生效**（反 AI 腔、过度声称护栏、术语保护、完整性底线见通用规范）。

> 定位：本规范回答「论文怎么写、怎么过审」。两阶段写作形式：阶段A 中文 MD 内容定稿（写 → 润色 → 内容 Review 循环）→ 阶段B 翻译英文 LaTeX（翻译 → 英文润色 → 翻译 Review 循环）；多角色：Writer / Polisher / Translator / Reviewer / 翻译 Reviewer / Editor。**论文素材全部来自流程二/三，写作不编造任何东西**。v2.0 起补充「主张-证据纪律」（§7）与「引用核验纪律」（§8）；v2.2 起与流程四工作流 v3.0 对齐（中文定稿后才翻译）。

## 0. 参考来源

| 来源 | 吸收内容 |
|------|----------|
| nature-writing（scholar/references/nature-writing/） | 论证先于句子、章节论证链、摘要/引言/相关工作/结果/讨论/结论写作规范（§3）、中文作者工作流 |
| ml-paper-writing（本地 skill） | Claim Ledger Gate（主张必须有证据记录支撑）、引用禁编造（程序化验证）、投稿前检查（§7-8） |
| paper-self-review（scholar/references/paper-self-review/） | 六维审查、Claim Audit（keep/weaken/revise/remove）、Final Verdict 三档、Top3 阻塞+Top3 打磨（§6） |
| review-response（本地 skill） | 证据锚点规则、原子问题拆分、完整性（所有意见都要回应）（§6） |
| nature-polishing / writing-anti-ai（本地 skill） | 学术语体、反 AI 腔、过度声称校准（经通用规范） |
| boardroom（ref-external/alirezarezvani-claude-skills） | 多角色评审隔离协议：独立意见 → 交叉审阅 → 合成裁决；显式 dissent（§4） |
| ai-development-team（ref-external） | 负面判据（问题未逐条清空不得声称完成）、review 循环只审「本轮修复」（§5-6） |
| superpowers/receiving-code-review（方法已吸收） | 处理意见纪律：先验证再实施、不表演性服从、不清楚先澄清、可技术反驳（§5） |
| latex-document-skill（ref-external） | LaTeX 编译与格式工程（引擎检测、多遍编译、错误解析） |
| agent-research-skills/latex-formatting（ref-external） | 会议模板（ICML/ICLR/NeurIPS/AAAI/ACL）、投稿前检查（§9） |
| 通用写作规范（standards/通用写作规范.md） | 反 AI 腔、过度声称、术语冻结、完整性底线 |

## 1. 写作目标

- 产出一篇**可发表**的英文论文：结构完整、逻辑自洽、证据支撑主张、语言自然；
- **核心贡献一句话能说清**（说不清 = 还没到写作阶段）；
- 所有内容可回溯：数字来自流程三实验，方法来自流程二方案，文献来自流程一；
- **每条主要主张都有证据锚点**：无锚点的主张要么补证据，要么削弱/删除（§7）。

## 2. 写作形式规范（两阶段，固定）

**阶段A：中文 MD 内容定稿（先不翻译）**

1. **写中文稿（Writer）**：内容与结构在中文稿阶段**全部定稿**（md，引用用 Obsidian wikilink）；
2. **润色中文（Polisher）**：语言打磨、术语统一，**不改内容与结论**；
3. **内容 Review（Reviewer/Editor）**：按六维审查，循环至总判定通过（写作问题清零、无研究层面问题）→ **中文稿定稿**；**未定稿前禁止翻译**。

**阶段B：翻译为英文 LaTeX + 翻译质量迭代**

4. **翻译（Translator）**：中文定稿 → 英文，**直接按格式模板写 `.tex`**；保持术语一致（术语表来自 02/03）、忠实不增删、结论强度不变；**翻译意图，不翻译句式**（中文句序按背景→方法→结论排列时，按英文章节要求重组，不逐句直译）；
5. **英文润色（Polisher）**：学术语体、句式流畅、反 AI 腔，在 `.tex` 上进行；
6. **翻译 Review（翻译 Reviewer/Editor）**：**只审翻译质量**（忠实度 / 术语 / 英文质量 / LaTeX 正确性），循环至通过；
7. **编译**：引用 `\cite`、图表 `\ref`、公式 `\eqref`，编译通过为底线；
8. **模板只约束格式**：版面/字体/章节/引用格式按模板，写作风格按本规范。

## 3. 结构规范

1. **摘要**：五要素齐全且自包含——背景/问题 → 缺口 → 方法 → 关键结果（含量化数字或对比）→ 边界化意义。诊断：以「Here, we」开头可能缺背景；以宽泛承诺结尾需收边界；全篇无数值/对比则显得无支撑；
2. **引言**：受控漏斗——领域重要性 → 现有实践的瓶颈 → 公平且具体地对待已有工作 → 剩余能力缺口 → 本研究直接回应此缺口。禁止：无收窄逻辑的文献罗列、靠贬低前人声称新颖、在读者理解问题前先报结果；
3. **相关工作**：按主题组织（2-4 个聚焦主题：任务主流方法 / 与核心思想最近的方法 / 依托的辅助技术），每个主题：主题句 → 代表性方法 → 与本课题相关的局限 → 引向本方法的过渡；最后推导研究空白。禁止逐篇枚举、隐藏强基线；
4. **方法**：问题形式化、算法设计、数学与理论（伪代码用 algorithm 环境）；方法前置于第 2-3 页；可复现为底线——读者能按论文重做；
5. **实验/结果**：用证据阶梯组织——系统/工作流概览 → 平台/设置可信度验证 → 主结果 → 与基线公平对比 → 机制/诊断分析 → 泛化/压力测试；每个子节以 `To test [question], we [action].` 开头；每个实验写明回答什么问题；主结果必须含量化数值；
6. **讨论**：从发现走向意义——中心进展 → 证据为何支持 → 与已有工作的关系 → 局限与依赖 → 未来工作；不复述每个图表，只选改变解读的证据；
7. **结论**：四段式收尾——核心贡献 → 决定性证据 → 意义 → 边界；不引入新数据、新引用、新机制。局限与任务目标/设置边界挂钩（数据域/假设/部署范围），不把可修的实现缺陷当结论局限。

## 4. 多角色职责规范

- **Writer（写手）**：负责内容与结构；每轮只处理本轮 review 指出的写作问题，不夹带私货；
- **Polisher（润色）**：负责阶段A中文稿与阶段B英文稿各一轮润色（语言、句式、术语、反 AI 腔）；不改变内容与结论；
- **Translator（译者）**：中英对照一致；术语表统一；不回译改义；翻译意图不翻译句式；
- **Reviewer（审稿人）**：站在审稿人立场按六维检查**中文稿**（阶段A内容 Review），输出结构化意见：`位置 / 问题 / 严重度 / 归类（写作 or 研究） / 证据锚点`；
- **第二 Reviewer / Devil's Advocate（可选）**：专门找拒稿风险点（贡献不足、对比不公、消融缺失、方法缺陷），与主 Reviewer **独立产出**（隔离，禁止先看对方意见）；
- **翻译 Reviewer（翻译审校，阶段B）**：**只审翻译质量**——忠实度（与中文定稿逐段对应、无漏译/误译/增删）、术语一致性、英文质量、LaTeX 正确性；**不审内容与研究问题**，发现内容/研究问题（阶段A漏检）返回阶段A处理；
- **Editor（主编）**：汇总、逐条归类、判严重度、出总判定；**最终由用户确认**；显式记录 dissent（未解决的异议）。

## 5. 迭代与存档规范

1. **论文工作目录 = `论文/<主题>/thesis/`**（模板整体副本 + 全部写作产物，不另建 `paper/`）：`main.tex`（模板主文件，阶段B直接编辑，保留模板封面/背景/章节结构）、模板资源（`.cls/.sty/图片/封面/背景`，**禁止删除/替换**）、`draft-zh.md`、`references.bib`、`main.pdf`，内含 `versions/` 子目录（历史版本 + review 反馈）；
2. 每轮 review 后把当轮定稿与 `review-vN.md`、`changelog-vN.md` 存入 `versions/vN/`，版本号连续递增、**只增不删**；
3. `changelog-vN.md` 必须逐条对应 review 问题，格式：`问题 → 修改 → 复核 → 证据`；未逐条解决并复核前不得声称完成（负面判据）；
4. 轮次定义：内容轮 = 「写中文 → 润色 → 内容 Review」（阶段A，产出中文定稿 md）；翻译轮 = 「翻译/润色英文 → 翻译 Review」（阶段B，产出英文 tex）；两轮共用同一版本序列，review 通过即进入下一阶段或停止；
5. **下一轮只修 review 指出的问题**（review the fix, not the whole paper）：改稿范围 = 上一轮问题清单，不夹带新内容；
6. **处理意见纪律**：不清楚的意见先澄清再改（部分理解 = 错误实现）；对错误意见用技术理由+证据反驳，不表演性服从；有争议的交用户裁决；
7. **连续两轮未解决同一问题** → 停止继续改稿，回根因（意见没说清？主张本身缺证据？），记录并询问用户。

## 6. Review 规范（六维 + 判定）

**六维检查（每轮必查）：**

1. **结构完整性**：摘要四要素（问题/方法/结果/贡献）、引言动机与缺口、方法可复现、结果支撑结论、讨论含局限；
2. **逻辑一致性**：研究问题与方法匹配、实验设计支撑假设、结果解读合理、结论由证据推出；
3. **主张-证据审计**（Claim Audit）：每条主要主张 → 判定 `keep / weaken / revise / remove` → 证据 → 缺失证据 → 过度声称风险 → 建议措辞；
4. **引用完整与准确**：所有引用在参考文献中存在、格式一致、关键相关工作已覆盖、引用准确反映原文（配合 `citation-verification`）；
5. **图表质量**：标题/图注清晰、支持正文叙事、格式符合目标期刊/会议要求；
6. **写作清晰**：语言简洁、术语恰当、句式清楚、段落组织有逻辑。

**严重度与判定：**

- 严重度：高（拒稿风险，必须解决）/ 中（应解决）/ 低（打磨）；
- 总判定三档（Final Verdict）：`ready with minor edits`（可发表，小修）/ `needs moderate revision`（需中修）/ `not ready for submission`（不可发表，缺研究内容 → 中断）；
- 报告必含：Top 3 阻塞问题、Top 3 打磨问题、缺失证据或缺失引用、图表风险。

**证据锚点规则：** 每条意见和每条回应必须挂一个锚点（论文位置 / 结果表图 / 引用或证据记录 / 计划实验及状态 / `unresolved`）；多原子点的问题先拆分再逐条处理；所有意见必须全部回应，不允许静默跳过。

**拒稿风险维度（Reviewer 参考）：** 贡献不足（失败案例太常见/技术已被充分探索）、写作不清（不可复现/模块缺动机）、实证效果弱（提升边际/绝对性能不够）、评估不完整（缺消融/缺强基线/数据集太简单）、方法设计有问题（设置不现实/技术缺陷/需逐场景调参）。

**翻译 Review（阶段B，只审翻译质量）：**

- 忠实度：英文与中文定稿逐段对应，无漏译 / 误译 / 增删信息；数字、符号、结论强度与中文定稿一致；
- 术语一致性：术语表（02/03）统一，未擅自替换冻结术语（通用写作规范 §3.1）；
- 英文质量：学术语体、语法、句式流畅、反 AI 腔；
- LaTeX 正确性：`\cite` / `\ref` / `\eqref` 映射齐全无悬空，格式符合模板；
- 总判定：通过 / 需再润色；**不审内容与研究问题**——发现内容/研究问题（阶段A漏检）返回阶段A处理，不在英文稿上直接改内容。

## 7. 主张-证据规范（写前与每轮 review 都执行）

- **Claim Ledger Gate**：任何主张进入论文前，先确认有证据记录支撑；计划/笔记中的假设不自动变成论文的结果主张；
- 用一句话写核心主张：`在 [系统/问题] 上，我们用 [方法] 展示了 [进展]，证据是 [实验]，边界是 [局限]`；
- 主要主张清单 → 每个主张标注证据锚点（`results/` 文件 / 图 / 表 / 01-03 章节）；
- 无锚点的主张：标注 `[CLAIM NEEDS EVIDENCE]` 并先处理（补证据或削弱/删除），**不润色无证据的主张**；
- 相关工作动机≠自己结果的证据；相关关系不写成因果关系；结论强度不超过证据（动词校准：show/demonstrate/suggest/indicate/may/could）。

## 8. 引用与核验规范（禁止编造）

- **绝不用记忆生成 BibTeX**（AI 生成引用错误率约 40%，编造参考文献 = 学术不端）；
- 引用必须程序化验证（CrossRef / arXiv / Semantic Scholar / 检索核验）或来自流程一核验过的文献库；
- 无法确认的引用：写 `\cite{PLACEHOLDER_author_year_verify}` 并明确告知用户「有 N 条待人工核对」，不猜测补全；
- 参考文献与正文一一对应，无幽灵引用、无漏引；引用准确反映原文内容（配合 `citation-verification`）。
- **md 稿文末参考文献（中文/英文稿）**：正文结束后加 `## 参考文献`，按编号升序列出（`[[论文/<主题>/<论文短名-年>/<md文件名>|[编号] <完整参考文献>]]`），编号与正文一一对应；转 LaTeX 时映射为 `\cite{key}` + `references.bib`（见 `standards/通用写作规范.md` §4）；

## 9. LaTeX 格式规范

- 引用：`\cite` + `references.bib`；图表：`\label`/`\ref` 对应；图注 `Figure N: 标题。说明`，表注 `Table N: ...`；
- 流程图 / 架构图源在 md 内（Mermaid，Obsidian 渲染）；进论文用导出 SVG/PNG（≥300 DPI）或矢量图 `\includegraphics`；
- 公式：编号公式用 `equation`/`align`，正文引用 `\eqref`；
- 编译：`python scripts/latex/compile_latex.py thesis/main.tex`（在模板工作目录内编译，输出 `thesis/main.pdf`，封面/背景随模板渲染）；交付前必须编译通过（无 Error、PDF 产出），并检查警告（Undefined reference/citation、overfull box）尽量清零；**禁止删除/替换模板自带资源（`.cls/.sty/图片/封面/背景`）**；
- 投稿前检查：页数限制、匿名要求（作者信息/致谢匿名化）、引用格式符合模板（参考 latex-formatting 思路）。

## 10. 完整性底线

1. 不编造文献、数据、指标、他人观点；
2. 数字必须与流程三 `results/` 一致；图表必须来自 `figures/`、`tables/`；
3. 结论强度不超过证据（通用规范 §2 过度声称护栏）；
4. 相关关系不写成因果关系；不暗示超出数据的普适性；
5. 负面结果、局限、缺失基线诚实呈现，不粉饰。

## 11. 检查清单（交付前）

- [ ] 核心贡献一句话能说清；主张-证据映射完成，无 `[CLAIM NEEDS EVIDENCE]` 悬空项
- [ ] 中文稿已定稿（阶段A内容 Review 通过后才翻译）；英文稿与中文稿一致（术语/信息/结论强度）
- [ ] 阶段A中文润色与阶段B英文润色各完成一轮（反 AI 腔自检）
- [ ] 摘要五要素齐全且含量化结果（不以 Here, we 开头缺背景、不以宽泛承诺结尾）
- [ ] 引言漏斗完整：背景→瓶颈→公平前人→缺口→本研究
- [ ] 方法前置于第 2-3 页，可复现（含假设与关键设计选择）
- [ ] 相关工作按主题组织，末尾有批判性小结与空白推导，未隐藏强基线
- [ ] 结果按证据阶梯组织；每个实验对应要回答的问题；基线诚实调参
- [ ] 讨论/结论：限制边界明确，无新数据新引用
- [ ] 数字与 results/ 一致，图表来自 figures/、tables/，图注完整
- [ ] md 稿引用均为 wikilink 可点击跳转，转 LaTeX 后与 `references.bib` 一一对应；引用全部程序化验证或标注 PLACEHOLDER 并已告知用户
- [ ] 内容 Review：六维检查完成，无研究层面问题，写作层面问题清零，Top3 阻塞/打磨已列（中文稿阶段）
- [ ] changelog 逐条对应（问题→修改→复核→证据），无未回应意见
- [ ] `versions/` 存档完整：每轮定稿 + review 报告 + changelog
- [ ] 翻译 Review 已通过（忠实度/术语/英文质量/LaTeX 正确性；未发现内容/研究问题）
- [ ] 编译通过（无 Error、无悬空引用），符合模板格式与投稿要求；`thesis/` 模板资源完整（封面/背景/类文件未被删除/替换）

## 更新记录

| 版本 | 日期 | 反馈 | 改动 |
|------|------|------|------|
| v1.0 | 2026-08-03 | 初始版本 | 建立中文先行→英文→LaTeX 写作形式、多角色循环（写/润色/翻译/review）、review 分流规则（研究问题停止返回前流程）、完整性底线 |
| v1.1 | 2026-08-03 | 用户补充：流程四按「写→润色→翻译→再润色→review」固定顺序循环；review 抛出非写作问题即中断；生成文件夹存放历史不同版本的 review 反馈并持续迭代 | 写作形式规范改为六步固定顺序（含中文润色与英文再润色各一轮）；新增 §5 迭代与存档规范（`paper/versions/` 只增不删）；Review 分流规则明确「中断 vs 下一轮循环」；检查清单增加存档项 |
| v2.0 | 2026-08-03 | 全面升级（参考 paper-self-review / nature-writing / ml-paper-writing / review-response / boardroom / receiving-code-review） | 新增 §7 主张-证据规范（Claim Ledger Gate / 证据锚点 / `[CLAIM NEEDS EVIDENCE]`）、§8 引用与核验规范（禁编造 / 程序化验证 / PLACEHOLDER）；§3 结构规范增强（论证链/引言漏斗/结果证据阶梯/结论四段式/局限分类）；§4 多角色职责补充独立评审隔离与 Devil's Advocate；§6 Review 规范（六维 + 严重度 + Final Verdict 三档 + 证据锚点 + 拒稿风险维度）；§5 补下一轮只修 review 问题与处理意见纪律；检查清单全面扩展 |
| v2.1 | 2026-08-04 | 用户补充：引用要全局可跳转 | 检查清单新增「md 稿引用 wikilink 可点击跳转 ↔ LaTeX `\cite` 映射」；与通用写作规范 §4（全局强制）对齐 |
| v2.2 | 2026-08-07 | 用户要求：先中文md迭代定稿再翻译成英文LaTeX，翻译阶段只做翻译review | §2 写作形式改为两阶段（阶段A中文MD内容定稿→阶段B翻译英文LaTeX+翻译Review循环）；§4 新增翻译 Reviewer 角色；§5 轮次定义拆分内容轮/翻译轮；§6 新增翻译 Review 范围（只审翻译质量）；检查清单同步 |
| v2.3 | 2026-08-07 | 用户反馈：模板没被真正使用，编译 pdf 丢失封面/背景 | 论文工作目录改为 `thesis/`（模板整体副本 + 全部写作产物，废除 `paper/`）：阶段B直接编辑 `thesis/main.tex` 保留模板封面/背景/类结构；编译在模板目录内输出 `main.pdf`；新增「禁止删除/替换模板资源」规则 |
