# 代码实现规范

版本：v1.2（2026-08-05）
适用：流程三 / 产出：按 `03_代码架构设计.md` 生成的代码文件与测试
执行方式：本规范 + `通用写作规范.md` **同时生效**（反 AI 腔、过度声称护栏、术语保护、完整性底线见通用规范）。

> 定位：本规范回答「代码怎么写才对」。与 02（方法论：算法+数学）、03（架构：怎么组织）配合：02 说用什么方法，03 说怎么拆，本规范说**每一行怎么写**。核心三指标与 03 一致：可测试、可维护、可读。v1.2 起补充「写之前的最小化决策（梯子模型）」（§2.1）与「写之后的简化审查」（§8），与流程三工作流 v2.1 对齐。

## 0. 参考来源

| 来源 | 吸收内容 |
|------|----------|
| everything-claude-code/coding-standards（ref-external/everything-claude-code） | 命名、不可变性、错误处理、KISS/DRY/YAGNI、代码味道（长函数/深嵌套/魔法数字）、AAA 测试结构 |
| wondelai/clean-code（ref-external/wondelai-skills） | 有意义命名、小函数、注释纪律、Boy Scout 规则 |
| superpowers/test-driven-development（方法已吸收） | TDD 铁律（无失败测试不写生产代码）、RED 失败原因必须正确、GREEN 后输出干净 |
| ponytail | https://github.com/DietrichGebert/ponytail | 实现前最小化决策（梯子模型：YAGNI→复用→标准库→原生→已装依赖→一行→最小实现）、懒于方案不懒于阅读、bug 修复=根因（grep 所有调用者）、延迟捷径注释台账、过度工程简化审查（§2.1 / §8） |
| superpowers/systematic-debugging（方法已吸收） | 系统化调试四阶段（根因→模式→假设→实施）、无根因不修复、3 次失败怀疑架构、防御纵深、条件等待（§6） |
| superpowers/verification-before-completion（方法已吸收） | 验证门禁（IDENTIFY→RUN→READ→VERIFY→CLAIM）、证据先于断言、回归红-绿-红-绿（§7） |
| qodex/system-design（ref-external/qodex-ai-agent-skills） | 早返回、函数 <80 行、文件 <200 行、库优先不重复造轮子 |
| awesome-copilot/write-coding-standards-from-file（ref-external/awesome-copilot） | 编码规范文档结构（命名/格式/注释/错误处理/提交评审/测试） |

## 1. 写作目标

- 把 03 架构文档变成**可运行、可测试、可维护、可读**的代码；
- 代码是给读者看的（读/写比 >10:1），优先为读者优化；
- 每个阶段产出「测试 + 实现 + 验证证据」三件套。

## 2. 实现原则

1. **架构一致**：目录即架构；接口签名与 03 一致；核心不依赖框架/DB（依赖规则不被破坏）；
2. **测试先行（TDD 铁律）**：无失败测试不得写生产代码；先看测试失败（且失败原因正确）→ 最小实现 → 重构；
3. **验证闭环**：完成声明必须附 fresh 验证输出；测试失败据错误信息改代码后重跑；
4. **系统化调试**：无根因调查不修复；禁止「随便改改再跑」「多改几处一起跑」；3 次修复失败即怀疑架构（§6）；
5. **最小化决策（梯子模型）**：写码前逐级爬梯子（YAGNI → 复用 → 标准库 → 原生 → 已装依赖 → 一行 → 最小实现），见 §2.1；
6. **库优先**：先找现成库/工具，不重复造轮子；**绝不为一两行功能新增依赖**；确需自写才自写（业务逻辑/性能关键路径/安全敏感）；
7. **Boy Scout 规则**：改动过的代码，离开时比来时更干净。

### 2.1 梯子模型（写码前的最小化决策，借鉴 ponytail）

**写任何生产代码前，先爬梯子，停在第一个成立的梯级**：

1. **这东西需要存在吗？**（YAGNI）投机需求跳过，一句话说明；
2. **代码库已有？** 复用（先找再写）；
3. **标准库能做？** 用它；
4. **原生平台功能能覆盖？** 用它；
5. **已装依赖能解？** 用它，绝不为一两行加新依赖；
6. **一行能解决？** 就一行；
7. **然后才写最小可行实现。**

- **懒于方案，绝不懒于阅读**：先通读任务涉及的代码、trace 真实流程，再选梯级；跳过理解的懒是危险的懒；
- **架构不砍，砍膨胀**：架构文档（03）定义的模块/接口必须实现；砍的是模块内部的投机抽象、冗余封装、多余配置；
- **安全底线（lazy, not negligent）**：信任边界校验、防数据丢失的错误处理、安全、无障碍、用户明确要求——绝不砍；
- **Bug 修复 = 根因**：修 bug 前 grep 所有调用者，在共享函数修一次；只补丁报告指出的路径 = 制造第二处 bug；
- **延迟捷径标注**：有意简化且有已知上限的，用 `# ponytail: <上限>, <升级路径>` 注释标注（见 §3.3），定期收集成台账，防止「以后再说」变成「永远不做」。

## 3. 编码规范

### 3.1 命名

- 命名揭示意图：变量/函数/类名能回答「为什么存在、做什么、怎么用」；
- 类名用名词，函数名用「动词 + 名词」；布尔用谓词式（`is_valid`、`has_permission`）；
- 一个概念一个词：不混用 `fetch / retrieve / get`；
- 避免无意义缩写与匈牙利命名；短变量名只用于极小作用域循环计数；
- 常量用 `UPPER_SNAKE_CASE`，文件/函数/变量按语言惯例（Python `lower_snake_case`）。

### 3.2 函数

- 函数小且单一职责：理想 4-6 行，最多不超过 80 行；超过就拆分；
- 参数尽量少（0-2 个）；一个抽象层级，不混合高层与低层；
- 早返回优于深嵌套（嵌套超过 3 层必须重构）；
- 文件超过 200 行拆分为多个文件。

### 3.3 注释

- 注释解释 **WHY**，不解释 WHAT（WHAT 由好命名表达）；
- 不写废话注释（`count++  # 计数器加1`）；
- 公共接口写简短 docstring（用途、参数、返回、抛错）；
- 遗留问题用标记：`TODO`（后续工作）、`FIXME`（已知错误行为）、`NOTE`（非显而易见的决策）；
- 延迟捷径用 `ponytail:` 标记：`# ponytail: <简化了什么>, <上限>, <升级路径>`（如全局锁 / 朴素启发式）；该标记会被收集进捷径台账，防止延期变成永久。

### 3.4 错误处理

- 显式处理错误，不静默失败（要么抛错，要么记录日志）；
- 外部调用（网络/IO）必须有错误处理与重试/超时；
- 错误消息可定位（包含上下文：哪个操作、什么状态码/原因）；
- 释放资源：异常路径也要清理（`with` / `try-finally`）。

### 3.5 不可变性与数据

- 默认不可变：不原地修改传入的数据结构（复制后再改）；
- 魔法数字改为命名常量（`MAX_RETRIES` 而非 `3`）；
- 不用 `any`/动态类型偷懒（有类型系统就写类型）。

### 3.6 代码味道（自检）

| 味道 | 症状 | 修复 |
|------|------|------|
| 长函数 | 函数 >80 行 | 按职责拆分 |
| 深嵌套 | if 嵌套 >3 层 | 早返回 / 提取函数 |
| 魔法数字 | 裸数字无命名 | 提取命名常量 |
| 重复代码 | 复制粘贴 | DRY 提取公共逻辑 |
| 注释废话 | 注释复述代码 | 删注释、改命名 |
| 上帝对象 | 一个类做所有事 | 按 SRP 拆分 |

## 4. 测试规范

- 测试结构用 **AAA**（Arrange 准备 / Act 执行 / Assert 断言）；
- 测试命名描述行为：`test_returns_empty_list_when_no_match`，不用 `test_works`；
- 一个测试只验证一个行为（名字里出现 and 就拆分）；
- 优先测真实代码，mock 只在不可避免时（外部服务/IO）使用；
- 测试确定性：随机性必须固定种子；
- 覆盖率要求：核心逻辑关键路径全覆盖；不做覆盖率崇拜，重点在行为验证；
- 测试也 YAGNI：平凡的一行逻辑无需测试；非平凡逻辑（分支/循环/解析/金额与安全路径）至少留一个 runnable check（`assert` 自检或一个 `test_*.py`）；不为投机功能写测试；
- 最小测试（一个自检 / 一个 test）不是膨胀，简化审查（§8）不得标记删除；
- 测试先红后绿：每个新功能先写失败测试并**确认失败原因正确**（功能缺失，而非语法/断言错误）；
- 修完 bug 必须补回归测试，并做「红-绿-红-绿」验证：通过 → 撤销修复**必须失败** → 恢复 → 通过（只写测试没验证「红」= 测试无效）。

## 5. 语言与表达（代码即文档）

- 命名与领域语言一致（与 02/03 文档同一术语，见通用规范 §3.1 术语冻结）；
- 提交说明 / 交付说明简洁可读：做了什么、验证了什么；
- 不写「看起来没问题」这类无证据描述，交付时给命令与输出。

## 6. 系统化调试（v1.1 新增）

**铁律：没有根因调查，不许提修复方案。** 对任何测试失败 / bug / 意外行为，先完成四阶段，再谈修复。

1. **根因调查**：读错误消息与堆栈（行号/文件/错误码）→ 稳定重现（记录步骤与频率）→ 检查最近改动（git diff / 新依赖 / 环境差异）→ 多组件系统在每层边界加诊断输出定位断裂层；
2. **模式分析**：找相似的正确实现对比 → 列出「正确 vs 错误」每个差异（不假设「那个不影响」）→ 理解依赖与假设；
3. **假设与最小验证**：写下具体假设「我认为 X 是根因，因为 Y」→ 只做最小改动验证该假设（一次一个变量）→ 无效则换新假设，不叠加修复；
4. **实施修复**：先写能复现该 bug 的失败测试 → 单一修复（不做「顺手」改进）→ 验证通过且无回归。

规则：

- **3 次修复失败**：停止修症状，怀疑架构问题（每次修复都暴露新耦合/状态问题 = 模式可能不对），记录并询问用户，不擅自重构；
- **防御纵深**：根因修复后，在相关边界加多层校验（重试/超时/日志/状态校验），防止同类问题复发；
- **条件等待**：环境/时序问题用「轮询条件成立」替代固定 sleep，带超时与错误信息；
- **修复位置 = 所有调用者的汇聚点**：修 bug 前 grep 该函数全部调用者；在共享函数加守卫（一次修复），不在每个调用点打补丁（借鉴 ponytail）；
- **真环境问题**（完成全部调查后）：记录调查过程与证据，加监控/日志后标记「已调查，待观测」，不轻易下「无根因」结论（95% 的「无根因」是不充分调查）。

## 7. 验证证据规则（v1.1 新增）

**铁律：没有 fresh 验证证据，不许声称完成。** 完成任何声明前执行五步门禁：

1. **IDENTIFY**：什么命令能证明这个声明？
2. **RUN**：执行完整命令（fresh 运行，不看历史输出）；
3. **READ**：读完整输出、看退出码、数失败数；
4. **VERIFY**：输出是否真的支持声明？否 → 如实报告实际状态；
5. **ONLY THEN**：才作声明。

- 证据必须**附输出**（命令 + 结果），不写「应该没问题 / 看起来正常 / 我确信」这类无证据描述；
- **红旗词**：should / probably / seems / 应该没问题 / 看起来正常——出现即回去验证；
- **回归验证**：修完 bug 用「红-绿-红-绿」验证回归测试有效（通过 → 撤销修复必须失败 → 恢复 → 通过）；
- 委托/子代理的结果也要独立验证（查 VCS diff + 跑测试），不信任自报「成功」。

## 8. 简化审查（过度工程审查，v1.2 新增）

**目标：diff 的最好结果是变短。** 每阶段 REFACTOR 后与交付前，对本次 diff 做一轮过度工程审查——只找「可以删什么」，一行一条发现，格式：`L<line>: <tag> <什么>。 <替代>`（多文件用 `<file>:L<line>: ...`）。

| tag | 含义 | 替代 |
|-----|------|------|
| `delete:` | 死代码、未用灵活性、投机功能 | 删掉，无替代 |
| `stdlib:` | 手写的标准库已有能力 | 指名标准库函数 |
| `native:` | 依赖/代码做了平台原生功能 | 指名原生能力 |
| `yagni:` | 单一实现的抽象 / 无人设置的配置 / 单调用层 | 内联，等第二个出现 |
| `shrink:` | 同样逻辑更少行 | 给出更短写法 |

- 结论用唯一指标收尾：`net: -<N> lines possible`；无可删则写 `Lean already. Ship.`；
- **范围边界**：只审过度工程与复杂度。正确性 bug / 安全漏洞 / 性能**不在本轮**（走 §6 调试与正常 review）；最小测试（一个自检 / 一个 test）不是膨胀，禁止标记删除；
- 简化审查**只列清单，不应用修改**；是否执行由实现者按清单逐条决定。

## 9. 检查清单


- [ ] 目录结构与 03 一致，核心不依赖框架/DB
- [ ] 接口签名与 03 接口表一致
- [ ] 每个新功能都有先红后绿的测试（失败原因正确）
- [ ] 所有测试通过并附 fresh 验证输出
- [ ] 修过的 bug 都有回归测试并通过红-绿-红-绿验证
- [ ] 调试遵循根因优先：没有「随便改改再跑」
- [ ] 命名揭示意图，无无意义缩写
- [ ] 函数 <80 行、文件 <200 行、嵌套 <3 层
- [ ] 无魔法数字（已提取常量）
- [ ] 注释只解释 WHY，公共接口有 docstring
- [ ] 外部调用有错误处理与超时/重试（条件等待优先于固定 sleep）
- [ ] 无重复代码、无上帝对象
- [ ] 库优先：没有重复造轮子
- [ ] 写码前已爬梯子（§2.1）：无投机抽象、无重复造轮子、无新增不必要依赖
- [ ] 有意简化且有上限的捷径已用 `ponytail:` 注释标注（§3.3）
- [ ] 本次 diff 已做简化审查（§8），结论附 `net` 行数
- [ ] 改动过的代码符合 Boy Scout 规则
- [ ] 通过通用规范反 AI 腔与过度声称自检（文档/交付说明部分）

## 更新记录

| 版本 | 日期 | 反馈 | 改动 |
|------|------|------|------|
| v1.0 | 2026-08-03 | 初始版本 | 吸收 coding-standards / clean-code / TDD / verification 规则，建立代码实现规范 |
| v1.1 | 2026-08-03 | 全面升级（参考 superpowers/systematic-debugging + verification-before-completion） | 新增 §6 系统化调试（根因四阶段 / 3 次失败怀疑架构 / 防御纵深 / 条件等待）、§7 验证证据规则（五步门禁 / 红旗词 / 回归红-绿-红-绿 / 不信任自报成功）、测试规范补回归验证、检查清单同步扩展 |
| v1.2 | 2026-08-05 | 融合 ponytail | 新增 §2.1 梯子模型（写码前最小化决策 / 懒于方案不懒于阅读 / 安全底线 / 根因修复 / 捷径台账注释）、§8 简化审查（tag 清单 + net 行数 + 范围边界）、§4 测试 YAGNI、§6 修复位置=调用者汇聚点、检查清单同步扩展；原 §8 检查清单改为 §9 |
