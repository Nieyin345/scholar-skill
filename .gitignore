# 密钥文件（禁止上传到 GitHub）
.env
*.env
!.env.example

# Python 缓存
__pycache__/
*.py[cod]

# 临时/测试产物
.paper-fetch-idem/
pdfs/

# LaTeX 编译产物（仅保留 .tex/.bib 源文件与模板示例 PDF）
*.aux
*.bbl
*.blg
*.log
*.out
*.fls
*.fdb_latexmk
*.synctex.gz
*.toc

# LaTeX 模板示例 PDF（编译产物，不提交）
references/latex-template/*.pdf

# 首次触发状态（本机级，不上传 GitHub）
state.json
state.local.json

# 机构认证会话 cookie（本机级，禁止上传 GitHub）
.scholar_institutional/
